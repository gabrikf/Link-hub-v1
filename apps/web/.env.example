# Base URL for the AI model CDN (e.g. CloudFront distribution).
# Leave empty to serve models from the same origin (default for local dev).
# Example: VITE_MODEL_CDN_BASE_URL=https://d1234abcd.cloudfront.net
VITE_MODEL_CDN_BASE_URL=
