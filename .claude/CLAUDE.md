<!--
  Intentionally empty of rules.

  The agent rules live in AGENTS.md at the repo root, which the root CLAUDE.md
  imports with a single `@AGENTS.md` line. Keeping the rules in one file means
  every tool that reads AGENTS.md — not just Claude Code — gets the same
  instructions, and there is no second copy to drift.

  Per-workspace depth: apps/api/AGENTS.md, apps/web/AGENTS.md.
  Do not add rules here. Add them to AGENTS.md.
-->
