# QA Run Report — 2026-08-22 — autonomous nightly loop, run `2026-08-22T17:17:51.062Z`

- **Scope:** Whole-product hardening round on `nightly/qa-hardening`. Intended to
  baseline the stack, hunt across the six lanes, triage, fix and regress. **The
  round did not get past BOOTSTRAP** — see *Why this round is incomplete*.
- **Cadence tier:** full (planned) → **sanity only** (actual)
- **Build:** `3c2b571` · **Environment:** web `http://localhost:5173` · api
  `http://localhost:3333` — **both ports were owned by a different application
  for the entire run** (see below). docker: `linkhub-postgres-dev` +
  `linkhub-redis-dev` healthy.
- **Seed data:** pre-existing (~307 users / 301 resume embeddings; seed password
  `12345678` for every seeded account). No `seed-all` was run this round.
- **Started:** 2026-08-22T17:17:51.062Z · **Ended:** 2026-08-22 (REPORT, iteration 2)
  · **Status:** closed

---

## Why this round is incomplete — read this first

The night **did not** end because the deadline hit, the budget ran out, or the
loop kept failing. It ended because **there was no LinkHub to test.**

Ports 3333 and 5173 were, and still are as this report is written, served by an
unrelated project — `weg/retro-doc` (package.json name `boilerplate`, page title
"Retro Doc — WEG"), started 13:09 from `/home/gabriel/Documents/www/weg/retro-doc`
via `turbo run dev --parallel`. Evidence, re-verified at REPORT time:

```
$ curl -s http://localhost:5173/ | grep -o '<title>[^<]*</title>'
<title>Retro Doc — WEG</title>

$ curl http://localhost:3333/profile/__nightly_probe__/posts
{"message":"Route GET:/profile/__nightly_probe__/posts not found","error":"Not Found","statusCode":404}
```

and from iteration 1: `POST /auth/login` → 404 despite
`apps/api/src/infra/http/routes/index.ts:65` registering it, `GET /docs/json`
returning an OpenAPI document full of `/api/v1/products/{productId}/jira/oauth/*`
paths that exist nowhere in this repo, and `ss -tlnp` showing both listeners with
a cwd under `weg/retro-doc`. No process anywhere was running `linkhub-v.1/apps/api`
or `apps/web`.

Consequence: **no e2e spec, no browser walk, no live API call against LinkHub ran
tonight.** `npx playwright test --project=desktop` dies in `[setup]`
(`authenticate recruiter` / `authenticate developer`, both HTTP 404 against the
wrong app) before a single journey executes — 0 of 42 tests carried signal.

Nothing was killed or restarted. The retro-doc processes are apparently-live work
belonging to another project.

### Addendum — resolved while this report was being written

Between the start and the end of the `REPORT` iteration, someone (not this loop)
landed commit `60547b7` — *"fix(nightly): refuse to QA the wrong app, and support
alternate ports"* — and brought LinkHub up on alternate ports. Verified directly:

```
$ curl -s http://localhost:5273/ | grep -o '<title>[^<]*</title>'
<title>LinkHub</title>
$ curl http://localhost:3344/profile/__nightly_probe__/posts
{"error":"RESOURCENOTFOUND","message":"User with identifier '__nightly_probe__' not found", ...}
```

That second response is the point: it is *LinkHub's own* error envelope, not
Fastify's "Route ... not found". **api 3344 / web 5273 is a real LinkHub.** Ports
3333 and 5173 are still retro-doc.

That commit also reports two further bugs found while wiring the alternate ports —
`playwright.config.ts` hardcoding `npm run dev:api` (EADDRINUSE against the other
project) and `scripts/visual/run.mjs` aborting the app's own requests because
`cli.config.json`'s origin allowlist names 5173/3333 literally — and claims 12
e2e tests passing plus a clean visual run on 3344/5273. **This round did not
verify any of that**; it is recorded here because it changes the recommendation
below, not because it was independently confirmed.

None of it retroactively tests this build. The round still walked zero journeys.

---

## Personas

| Persona | Base | Device / Network / Theme | Sessions |
|---|---|---|---|
| Nina, Diego, Priya, Atlas, Sam | `docs/qa/personas.md` | — | **none walked this round** |

No persona session was run. The tree that defines them (`docs/qa/personas.md`)
was created *by* this round.

## Flows in Scope

All five load-bearing journeys were in scope and **none were walked**:

- `J-signup-resume` — a developer registers and imports a resume
- `J-agent-posts` — an agent publishes; the human reviews; disclosure holds
- `J-recruiter-search` — semantic search + AI Match %
- `J-link-sharing` — Linktree-style link page
- `J-profile-appearance` — layout editor until the profile looks right

## Session Matrix & Results

| # | Charter | Journey / Scenario | Persona | Tour | Themes walked | Status | Issue | Fix commit |
|---|---|---|---|---|---|---|---|---|
| 1 | CH-bootstrap-gate | — / gate + unit baselines | — | — | n/a (no browser) | Pass | | |
| 2 | CH-bootstrap-e2e | all five journeys | — | — | none | **Blocked (needs human verify)** | port conflict | |
| 3 | CH-hunt-* (six lanes) | all | all | all | none | **Skipped** — no app to hunt | | |

Status legend: `Pending | Pass | Fixed | Skipped | Blocked (needs human verify) | Blocked (human decision)`

## Session Debriefs

### CH-bootstrap-gate — no persona (machine baseline)

- **Ran:** 2026-08-22T17:17:53Z → 17:27:07Z (9m14s; box respected: yes)
- **Themes:** n/a — no browser surface reachable
- **Findings:** the gate and every offline suite are green at `3c2b571`. These
  suites do not need the dev servers; they build their own in-memory Fastify via
  `build-test-app.ts`.

  ```
  npm run build:schemas                 → clean tsc build
  bash db-manage.sh status              → postgres + redis both "healthy"
  node scripts/guardrails/pre-push.mjs  → guardrails PASS
                                          (i18n-parity: skipped, no locales yet)
  ```

  | Package | Files | Tests | Failed |
  |---|---|---|---|
  | `apps/api` | 104 | 869 | 0 |
  | `apps/web` | 47 | 436 | 0 |
  | `@repo/schemas` | 6 | 105 | 0 |
  | `apps/training` | 9 | 87 | 0 |
  | extractor | 6 | 100 | 0 |

  **These are the real unit/integration baseline for future REGRESSION runs.**
  The `seed_baseline` block in `.nightly/QUEUE.json` covers e2e totals only.
- **Edge cases attempted:** confirmed the auth.setup failures were a wrong-server
  artifact and not product bugs, before aborting the e2e leg. That negative
  result is the main output of the session.
- **Bugs filed/updated:** none. The port conflict is not a LinkHub code defect,
  so it was deliberately **not** filed as a queue candidate — there is no fix in
  this repo for "another repo's dev server owns my ports".
- **Scenarios settled:** none
- **Paper cuts:** none observable — no UI was reachable
- **Surprises:** mid-iteration, `scripts/nightly/run.sh` and `scripts/visual/run.mjs`
  changed on disk (not by the agent) to add exactly the missing protection — a
  `verify_is_linkhub()` preflight probe, a FATAL early exit before the gate runs,
  and `--api-port` / `--web-port` overrides threading through `PORT`,
  `WEB_APP_URL`, `VITE_API_URL` and `--port --strictPort`. Both files are still
  **uncommitted** and were left that way: they are someone else's in-progress
  edit, not this round's change.
- **Suggested next charter:** re-run BOOTSTRAP against a verified LinkHub — either
  after stopping retro-doc, or with `--api-port 3433 --web-port 5273`.

## Disclosure Findings

No agent-authored surface was walked this round — the MCP surface, the review
queue and the public profile were all unreachable. **Zero disclosure edge cases
were attempted.** The disclosure lane is entirely unverified for this build.

One *static* disclosure-adjacent observation carried over from the hand-off queue,
not walked tonight: `CAND-0103` — `create_commit_summary_post`'s tool description
tells agents `repo` is publishable, but `publicPostResponseSchema` omits
`metadata` entirely, so it is never served and disclosure never scans it. The
docs and the behaviour disagree in the *safe* direction today.

## Theme Coverage

| Surface / route | Light | Dark | Notes |
|---|---|---|---|
| — | — | — | **No surface walked in any theme.** Zero of the app's routes were rendered. |

## What Was Fixed

**Nothing.** No FIX iteration ran, so there is no red commit, no green commit and
no regression test to verify. The two-commit protocol was never exercised this
round.

## Paper Cuts

None recorded — no persona reached a screen.

## Runtime Errors Observed

None observed against LinkHub. The 404s recorded above are the wrong application
answering, not LinkHub errors.

## Human Verifications Needed

- [ ] **Re-run the loop against 3344 / 5273** (see *Decisions for a Human* — the
      port conflict is now worked around, not still blocking). Every leg below
      needs that run.
- [ ] **Review `60547b7`** — it landed unreviewed during this round and it changes
      how the loop locates the app.
- [ ] Walk all five journeys in both themes. None were walked. (rows #2, #3)
- [ ] Confirm the seven `confirmed` bugs still reproduce on the current build —
      they were confirmed at hand-off, before iteration 1, and nothing tonight
      re-verified them.

## Decisions for a Human

### Ports 3333 / 5173 are owned by `weg/retro-doc` (no bug id — not a LinkHub defect)

- **What's broken:** the nightly loop's core premise. The orchestrator
  health-checks that *something* answers on 3333 and 5173 and treats a response
  as "dev servers up". Another Vite/Node project on its default ports satisfies
  that check perfectly, so the loop ran a full BOOTSTRAP against the wrong
  application.
- **Why not auto-fixed:** killing another project's running dev servers is not
  the loop's call, and the ports cannot be rebound while occupied.
- **Largely resolved during this iteration** — see the addendum above. `60547b7`
  landed the identity preflight and the `--api-port` / `--web-port` plumbing, and
  LinkHub is now verified live on 3344 / 5273. What remains is one decision:
- **Options:**
  1. **Re-run on the alternate ports now:**
     `bash scripts/nightly/run.sh start --hours 8 --fresh --api-port 3344 --web-port 5273`.
     Nothing else has to move; the servers are already up and verified.
  2. **Stop retro-doc and re-run on the defaults.** Restores the documented
     3333/5173 setup, so nothing depends on remembering two flags — at the cost of
     someone else's running work.
- **Recommendation:** **1.** The ports are up, identity-verified, and `60547b7`
  reports the e2e and visual runners already working against them. Take the free
  re-run tonight rather than spending it renegotiating ports. Do read `60547b7`
  before trusting it — this round neither authored nor reviewed that change.

## Learnings

- A liveness probe that only asks "is anything listening?" is not a liveness
  probe. It must assert **identity** — a route only this application serves. The
  in-flight `verify_is_linkhub()` (`GET /profile/__nightly_probe__/posts`, real
  response vs. Fastify's "Route ... not found") is the right shape.
- Vite's default 5173 and the conventional alt-API 3333 are common enough that
  collision with an unrelated repo should be treated as expected, not exotic.
- Routing to `REPORT` instead of grinding `HUNT` against a wrong target was the
  correct call and cost the run ~7.7 hours of unspent deadline rather than ~7.7
  hours of fabricated findings.
- The offline half of the stack (gate + five unit suites) is fully testable with
  no dev server at all. A future BOOTSTRAP should record that baseline *first*,
  as this one did, so a blocked night still produces one durable number.

## Final Status

- **Exit gate (`npm run build:schemas && node scripts/guardrails/pre-push.mjs`):**
  `guardrails PASS` (i18n-parity skipped — no locale files exist yet, expected)
- **Issues by user impact:** Blocks-Completion 2 · Data-Loss 1 · Trust-Damage 3 ·
  Friction 1 · Cosmetic 0 — **all seven carried in from the hand-off, none found
  or re-verified tonight**
- **Issues by Root Cause:** api-contract 1 · null-data 2 (error states) ·
  auth-permission 1 · layout-responsive 1 · third-party/framework-dependent 1
  (url scheme) · race-loading 1 (unhandled rejection)
- **Disclosure leaks found:** none — and **zero disclosure edge cases were
  attempted**, which is not the same as clean
- **Coverage:** 0 of 5 journeys walked. All skips disclosed above.
- **Theme coverage:** 0 surfaces walked in either theme; every gap named.
- **Parity gaps:** docker stack healthy and seeded; `OPENAI_API_KEY` legs never
  reached; the AI resume-parse spec (`01-signup-resume.spec.ts:353`) stays gated
  behind `E2E_ALLOW_AI_SPEND`; **the entire browser/API surface was parity-broken
  because the ports served a different app.**
- **Not visually verified:** every screen. No browser session touched LinkHub.
- **Verdict:** **not ready** — two open blockers and four majors are unfixed and
  unverified; clear the port conflict and re-run before treating any of this
  build as tested.
